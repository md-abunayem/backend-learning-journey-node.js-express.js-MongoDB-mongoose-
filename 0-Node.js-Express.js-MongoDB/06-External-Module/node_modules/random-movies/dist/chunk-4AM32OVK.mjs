// src/lib/pickOne.ts
var pickOne = (items) => {
  if (!items || !items.length) {
    throw Error("Invalid items");
  }
  return items[Math.floor(Math.random() * items.length)];
};
var pickOne_default = pickOne;

export { pickOne_default };
