import { movies } from './chunk-6PVQY675.mjs';
import { pickOne_default } from './chunk-4AM32OVK.mjs';
import { pickSome_default } from './chunk-AAANZKAX.mjs';

// src/index.ts
var randomTitle = (n) => {
  if (n) {
    return pickSome_default(movies, n).map((e) => e.title);
  }
  return pickOne_default(movies).title;
};
var randomDesc = (n) => {
  if (n) {
    return pickSome_default(movies, n).map((e) => e.description);
  }
  return pickOne_default(movies).description;
};
var randomMovie = (n) => {
  if (n) {
    return pickSome_default(movies, n);
  }
  return pickOne_default(movies);
};

export { randomDesc, randomMovie, randomTitle };
